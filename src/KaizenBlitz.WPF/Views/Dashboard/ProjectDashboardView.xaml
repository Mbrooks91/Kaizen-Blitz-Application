<Page x:Class="KaizenBlitz.WPF.Views.Dashboard.ProjectDashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Project Dashboard">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" 
                      Text="Project Dashboard" 
                      FontSize="28" 
                      FontWeight="Bold"
                      Foreground="{StaticResource TextDarkBrush}"/>
            
            <Button Grid.Column="1"
                    Content="NEW PROJECT"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding NewProjectCommand}"
                    Height="40"
                    Padding="24,0"/>
        </Grid>

        <!-- Search and Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Search projects..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,16,0"
                     Height="40"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Content="All" Command="{Binding FilterProjectsCommand}" Width="80"/>
                <Button Content="In Progress" Command="{Binding FilterProjectsCommand}" Width="100"/>
                <Button Content="Completed" Command="{Binding FilterProjectsCommand}" Width="100"/>
            </StackPanel>
        </Grid>

        <!-- Projects Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Projects}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White"
                                BorderBrush="#E5E5E5"
                                BorderThickness="1"
                                CornerRadius="4"
                                Margin="0,0,16,16"
                                Width="280"
                                Padding="16"
                                Cursor="Hand">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="8" ShadowDepth="2"/>
                            </Border.Effect>
                            
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" 
                                          FontSize="16" 
                                          FontWeight="Bold" 
                                          TextTrimming="CharacterEllipsis"
                                          Margin="0,0,0,8"/>
                                
                                <TextBlock Text="{Binding Description}" 
                                          FontSize="12" 
                                          Foreground="{StaticResource TextGray}"
                                          TextTrimming="CharacterEllipsis"
                                          Height="40"
                                          TextWrapping="Wrap"
                                          Margin="0,0,0,12"/>
                                
                                <ProgressBar Value="{Binding ProgressPercentage}" 
                                            Minimum="0" 
                                            Maximum="100"
                                            Height="4"
                                            Margin="0,0,0,8"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" 
                                              Text="{Binding Status}" 
                                              FontSize="11"
                                              Foreground="{StaticResource TextLight}"/>
                                    
                                    <TextBlock Grid.Column="1" 
                                              Text="{Binding ModifiedDate, StringFormat='MM/dd/yyyy'}" 
                                              FontSize="11"
                                              Foreground="{StaticResource TextLight}"/>
                                </Grid>
                                
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                                    <Button Content="Open" 
                                           Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding}"
                                           Margin="0,0,8,0"/>
                                    <Button Content="Delete" 
                                           Command="{Binding DataContext.DeleteProjectCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Visibility="{Binding Projects.Count, Converter={StaticResource BoolToVisConverter}}">
            <TextBlock Text="No projects found" 
                      FontSize="18" 
                      Foreground="{StaticResource TextLight}"
                      HorizontalAlignment="Center"/>
            <Button Content="Create Your First Project" 
                   Command="{Binding NewProjectCommand}"
                   Style="{StaticResource PrimaryButton}"
                   Margin="0,16,0,0"
                   HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
