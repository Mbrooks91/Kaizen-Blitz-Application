<Page x:Class="KaizenBlitz.WPF.Views.Tools.ActionPlanView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Action Plan">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Action Plan" FontSize="24" FontWeight="Bold"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="ADD TASK" Command="{Binding AddTaskCommand}" Margin="0,0,8,0"/>
                <Button Content="SAVE" Command="{Binding SaveCommand}" Margin="0,0,8,0"/>
                <Button Content="EXPORT EXCEL" Command="{Binding ExportExcelCommand}" Margin="0,0,8,0"/>
                <Button Content="MARK COMPLETE" Command="{Binding MarkCompleteCommand}"/>
            </StackPanel>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" 
                 ItemsSource="{Binding Tasks}"
                 AutoGenerateColumns="False"
                 CanUserAddRows="True"
                 CanUserDeleteRows="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Task Description" 
                                   Binding="{Binding TaskDescription}" 
                                   Width="*"/>
                <DataGridTextColumn Header="Responsible Person" 
                                   Binding="{Binding ResponsiblePerson}" 
                                   Width="150"/>
                <DataGridTemplateColumn Header="Deadline" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding Deadline}" BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridComboBoxColumn Header="Status" 
                                       SelectedItemBinding="{Binding Status}" 
                                       Width="120">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding Source={x:Static enums:TaskStatus}}"/>
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="Priority" 
                                   Binding="{Binding Priority}" 
                                   Width="80"/>
                <DataGridTextColumn Header="Notes" 
                                   Binding="{Binding Notes}" 
                                   Width="200"/>
                <DataGridTemplateColumn Header="Actions" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" 
                                   Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                   CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
